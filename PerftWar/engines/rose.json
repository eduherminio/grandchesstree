{
  "name": "rose",
  "version": "main",
  "owner": "87flowers",
  "repo": "https://github.com/87flowers/Rose",
  "language": "C++",
  "_note": "Rose is a UCI engine but its perft command is a standalone (NOT `go perft`) and the implementation is pure recursive single-threaded with no TT — so only single-no-cache is defined. We pass `bulk` so depth-1 leaves count via moves.size() (the same optimisation other engines use).",
  "modes": {
    "single-no-cache": {
      "launch": "bin/rose/rose",
      "setup": [
        "setoption name Threads value 1",
        "setoption name Hash value 1"
      ],
      "case":   "position fen {fen}\nperft {depth} bulk",
      "end_re": "^perft to depth \\d+ complete in",
      "quit":   "quit"
    }
  }
}
