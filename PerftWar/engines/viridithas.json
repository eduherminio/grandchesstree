{
  "name": "viridithas",
  "version": "20.0.0-dev",
  "owner": "cosmobobak",
  "repo": "https://github.com/cosmobobak/viridithas",
  "language": "Rust",
  "_note": "Viridithas is a UCI engine in Rust. `go perft N` produces a divide listing followed by a single `info depth N nodes K time T nps M` summary line. NNUE is embedded at build time from viridithas-networks. Only single-no-cache is defined.",
  "modes": {
    "single-no-cache": {
      "launch": "bin/viridithas/target/release/viridithas",
      "setup": [
        "setoption name Threads value 1",
        "setoption name Hash value 1"
      ],
      "case":   "position fen {fen}\ngo perft {depth}",
      "end_re": "^info depth \\d+ nodes \\d+ time \\d+ nps \\d+$",
      "quit":   "quit"
    }
  }
}
